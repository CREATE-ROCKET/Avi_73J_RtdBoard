#include <Arduino.h>

#include <SPICREATE.h>

#include "infrastructure/router.h"

void loop()
{
    // Data a;
    // a.id = 1;
    // uint8_t addr = 0;
    // Router1.flashController->Add(addr, a);
}
